# Colorado income tax — CO DOR flat personal income tax rate, annual (staggered). Flat-rate state, the PA workflow is the template. BUILD-SPEC.md §7.2, §8.
name: Refresh Colorado income tax

on:
  schedule:
    - cron: "0 12 22 2 *"
  workflow_dispatch:

jobs:
  refresh:
    uses: ./.github/workflows/_data-refresh.yml
    with:
      group: state-co
      source_name: Colorado income tax
