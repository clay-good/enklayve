# Dependencies
node_modules/

# Build output
dist/
.wrangler/

# Test + coverage
coverage/
test-results/
playwright-report/

# In-flight note from `npm run check:boundaries`, which rewrites src/engine in
# place. Present only while a run is mid-mutation; the next run clears it.
scripts/.boundary-mutation.json

# Editor / OS
.DS_Store
*.log
.idea/
.vscode/

# Env / secrets
.env
.env.*
!.env.example

# openlore analysis artifacts (regenerated by `npx openlore analyze`)
.openlore/
ARCHITECTURE.md
openspec/
